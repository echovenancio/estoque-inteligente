// Bridge so Expo (when started from repository root) can find the app entry.
// It re-exports the app implementation inside `mobile-react/App`.
import App from './mobile-react/App';

export default App;
